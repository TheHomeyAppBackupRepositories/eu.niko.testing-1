import { ZigBeeDevice } from 'homey-zigbeedriver';
import { ZCLNode } from 'zigbee-clusters';
export default function initPowerConfigurationDevice(device: ZigBeeDevice, zclNode: ZCLNode, capabilityId?: string, endpointId?: number): Promise<void>;
