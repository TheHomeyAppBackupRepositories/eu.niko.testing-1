import { ZigBeeDevice } from 'homey-zigbeedriver';
import { ZCLNode } from 'zigbee-clusters';
export default function initMeasureTemperatureDevice(device: ZigBeeDevice, zclNode: ZCLNode, capabilityId?: string, endpointId?: number): Promise<void>;
