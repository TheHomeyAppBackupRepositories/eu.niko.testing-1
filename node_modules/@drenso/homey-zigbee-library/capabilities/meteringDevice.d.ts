import { ZCLNode } from 'zigbee-clusters';
import { ZigbeeFactorDevice } from '../lib/helper/deviceFactor';
export default function initMeteringDevice(device: ZigbeeFactorDevice, zclNode: ZCLNode, endpointId?: number): Promise<void>;
