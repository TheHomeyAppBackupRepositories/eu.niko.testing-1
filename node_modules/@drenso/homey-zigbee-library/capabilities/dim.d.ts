import { ZigBeeDevice } from 'homey-zigbeedriver';
import { ZCLNode } from 'zigbee-clusters';
export default function initDimDevice(device: ZigBeeDevice, zclNode: ZCLNode, capabilityId?: string, maxDimValue?: number, minChange?: number, endpointId?: number): Promise<void>;
