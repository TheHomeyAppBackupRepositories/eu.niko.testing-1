"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const zigbee_clusters_1 = require("zigbee-clusters");
const attributeDevice_1 = require("../lib/attributeDevice");
async function initOnOffDevice(device, zclNode, capabilityId = 'onoff', endpointId) {
    const set = (value) => value ? 'setOn' : 'setOff';
    // Return empty object, the command specifies the action for this cluster ('setOn'/setOff')
    const setParser = () => ({});
    const reportParser = (value) => value;
    await (0, attributeDevice_1.initReadWriteCapability)(device, zclNode, capabilityId, zigbee_clusters_1.CLUSTER.ON_OFF, set, setParser, 'onOff', reportParser, undefined, endpointId);
}
exports.default = initOnOffDevice;
